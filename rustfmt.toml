#format_strings = true # nightly only
#imports_layout = "HorizontalVertical" # nightly only
#imports_granularity = "Crate" # nightly only
#group_imports = "StdExternalCrate" # nightly only
reorder_imports = true
reorder_modules = true
max_width = 120
